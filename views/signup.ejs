<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>新規登録画面</title>
    <link rel="stylesheet" href="/style.css">
    <script src="main.js"></script>
    <script src="/send_url.js"></script>
</head>

<body>
    <div class="sign">
        <div class="container">
            <div class="panel">
                <h2>新規登録</h2>
                <% if (errors.length> 0) { %>
                    <ul class="errors">
                        <% errors.forEach(error=> { %>
                            <li>
                                <%= error %>
                            </li>
                            <% }); %>
                    </ul>
                    <% } %>
                        <form action="/signup" method="post">
                            <p>ユーザー名</p>
                            <input type="text" name="username">
                            <p>ユーザーレベル</p>
                            <input type="button" value="push" onclick="js_number(); this.disabled = true;">
                            <input type="hideen" name="user_level" id="num54">
                            <p>メールアドレス</p>
                            <input type="text" name="email">
                            <p>パスワード</p>
                            <input type="password" name="password">
                            <input type="submit" value="登録する">
                        </form>
            </div>
            <p>アカウントをお持ちの場合<a href="/login">ログインする</a></p>
        </div>
    </div>
</body>

</html>